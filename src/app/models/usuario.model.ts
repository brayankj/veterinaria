export class User {

    constructor(
        names: string,
        email: string,
        password?: string,
        lastnames?: string,
        active?: boolean,
        years?: number,
        image?: string,
        google?: boolean,
        role?: string,
        id?: string,
    ) { }

}